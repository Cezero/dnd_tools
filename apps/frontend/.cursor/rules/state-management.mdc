---
description: Managing shared state across multiple components
globs: 
alwaysApply: false
---
Use React Context or a modern state library like Zustand; avoid excessive prop drilling
Use React Hooks like `useState`, `useEffect`, `useCallback`, and `useMemo`, and avoid class components
